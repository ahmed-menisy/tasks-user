import { ErrorhandleDirective } from './errorhandle.directive';

describe('ErrorhandleDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorhandleDirective();
    expect(directive).toBeTruthy();
  });
});
